# Лабораторная работа №18: Сравнение синтаксиса C# и JavaScript

## Основная информация
**ФИО:** KuzVah 
**Группа:** ИСП-233 
**Дата:** 26.02.2026

## Описание
В ходе выполнения лабораторной работы было изучено сравнение синтаксиса языков программирования C# и JavaScript. Рассмотрены основные различия в типизации, компиляции, объявлении переменных, работе с условиями, циклами, функциями и объектами. Выявлены сильные и слабые стороны каждого языка.

## Структура проекта
├── README.md # Документация лабораторной работы
├── Program.cs # Пример кода на C#
├── script.js # Пример кода на JavaScript
## Сравнительная таблица: JavaScript vs C#

| Аспект              | C#                                      | JavaScript                                   |
|---------------------|-----------------------------------------|----------------------------------------------|
| Типизация           | Статическая                             | Динамическая                                 |
| Компиляция          | Да (в IL-код)                           | Нет (интерпретация/JIT)                      |
| Точка входа         | `Main()`                                 | Отсутствует                                   |
| Переменные          | `int x = 5;`                             | `let x = 5;`                                 |
| Константы           | `const int X = 5;`                       | `const X = 5;`                               |
| Вывод в консоль     | `Console.WriteLine()`                    | `console.log()`                              |
| Проверка типа       | Во время компиляции                      | `typeof`                                      |
| Сравнение           | `==` (по значению)                       | `===` (строгое)                               |
| Условия             | `if/else/switch`                         | `if/else/switch` + тернарный оператор        |
| Циклы               | `for/while/do-while/foreach`             | `for/while/do-while/for...of`                |
| Функции             | Методы с типами                          | Функции без типов                             |
| Массивы             | `int[] arr = new int[5];`                | `let arr = [];`                              |
| Объекты             | Требуют класс                            | Можно создать литералом                       |
| ООП                 | Классическое                             | Прототипное                                   |
| Null                | `null`                                   | `null` и `undefined`                          |
| Строгость           | Высокая (компилятор)                     | Низкая (нужна дисциплина)                    |

## Главные выводы

1. **JavaScript гибче C#** — можно менять типы переменных во время выполнения, что упрощает быструю разработку, но требует внимательности.

2. **JavaScript проще синтаксически** — меньше "церемоний" при объявлении переменных, функций и объектов.

3. **JavaScript опаснее** — ошибки часто обнаруживаются только во время выполнения программы, что повышает риск скрытых багов.

4. **C# безопаснее** — строгая типизация и компилятор предотвращают множество ошибок ещё на этапе написания кода.